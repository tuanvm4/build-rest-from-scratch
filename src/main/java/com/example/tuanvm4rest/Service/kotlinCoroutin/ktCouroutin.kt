package com.example.tuanvm4rest.Service.kotlinCoroutin
import kotlin.coroutines.*
class ktCouroutin {

    /*// Simulate a function that fetches data from a remote server
    suspend fun fetchDataFromServer(): String {
        delay(2000) // Simulate network delay
        return "Data from server"
    }

    // Extension function to handle the result
    fun String.handleResult() {
        println("Received data: $this")
    }

    fun main() {
        // Use the runBlocking coroutine builder to start a coroutine in the main function
        runBlocking {
            println("Coroutine started")

            // Start a coroutine to fetch data from the server
            val data = async {
                fetchDataFromServer()
            }

            // Use the extension function to handle the result
            data.await().handleResult()

            println("Coroutine finished")
        }
    }*/

}